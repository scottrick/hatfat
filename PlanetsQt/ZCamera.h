#ifndef ZCAMERA_H
#define ZCAMERA_H

class ZCamera
{
public:
    ZCamera();

    static void doTempCameraCrap(float fCameraDistance);
    static void doTempPerspectiveCrap(float ratio);
};

#endif // ZCAMERA_H
